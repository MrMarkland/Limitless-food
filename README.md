# GitHub Folder

Upload the contents of this folder to the root of the GitHub repository.

Before publishing, edit `config.js` and set:

- `SCRIPT_URL` to the Apps Script deployment URL ending in `/exec`.
- `APP_TOKEN` to the exact token in Apps Script `Config.gs`.

Then enable GitHub Pages from the main branch and root folder.
